# TODO アプリ

依存関係なしの単一ファイル TODO アプリ。データはブラウザの localStorage に保存されます。

## 使い方

`index.html` をブラウザで開くだけです。

```bash
open index.html
```

## 機能

- タスクの追加 / 完了切り替え / 削除
- フィルター（すべて / 未完了 / 完了）
- 完了済みタスクの一括削除
- localStorage による自動保存
